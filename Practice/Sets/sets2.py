marks = {96, 97, 97, 97, 99}
for score in marks:
    print(score)

# if we use print(score) we will get each individual element
# if we print marks we will get everything
# since set is unique duplicate elements will only print once

